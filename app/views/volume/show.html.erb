<h2><%= @volume.name %> &mdash; <%= @volume.start_year %></h2>
<ul>
	<% @volume.issues.each do |issue| %>
		<li><a href="<%= "/volumes/#{@volume.id}/#{issue.issue_number}" %>"><%= "##{issue.issue_number} - #{issue.name}" %></a></li>
	<% end %>
</ul>